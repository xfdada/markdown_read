# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# PyInstaller spec (auto-generated, not needed in VCS)
*.spec

# Runtime config — contains file_key + password_hash (secrets)
# Use config.template.json as the committable version
config.json
