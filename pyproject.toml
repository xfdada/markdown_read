[project]
name = "markdown-read"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "bcrypt>=5.0.0",
    "flask>=3.1.3",
    "markdown>=3.10.2",
    "pygments>=2.20.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
